# Example site parameters
# Copy relevant sections to your main config
logo: '/images/logo/keno.png'
names:
  organization: 'Kenolab'
  product: 'Pentesting Labs'
version: 
  major: 0
  minor: 0
  patch: 3

theme:
  mode: dark

favicon:
  ico: "/favicon.ico"
  svg: "/favicon.svg"
  appleTouchIcon: "/apple-touch-icon.png"

article:
  childArticles:
    display: true
    count: 7 
    titleTrunc: 90
    descTrunc: 90
  nextPrev: 
    display: true
    trunc: 35
  chat: 
    display: false
  # Sidebar TOC (right sidebar)
  toc: 
    display: true
  # Inline components (within article content)
  inlineToc:
    display: false
    showProgress: false
  pageResources:
    display: true
  # Content status warnings (draft and expiry alerts)
  statusWarnings:
    display: true
  # Enhanced metadata section (dates, author, reading time, word count)
  metadata:
    display: true
  # Related content section (shows related pages in same section)
  relatedContent:
    display: true

# Default notebook repository configuration
notebook:
  # Default repository URL for notebooks (can be overridden per page)
  defaultRepoUrl: "https://github.com/nvidia/notebook-examples"
  # Default branch (can be overridden per page)
  defaultBranch: "main"

# Development & Debug Settings
debug: false  # Enable debugging to see what parameters are loaded
